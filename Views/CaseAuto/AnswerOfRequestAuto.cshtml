@{
    Layout = "_Layout";
    @using CaseAutoService.Model;
}

<table class="table">
   
    <tr>
        <td>Name</td>
        <td>Manufacturer</td>
        @foreach (var Auto in ViewBag.List)
        {
            <tr><td>@Auto.Name</td> <td>@Auto.Manufacturer</td> </tr>
        }
    </table>

